var path = require('path')

const filesConfig = {
    resourcesFolder: path.join('resources'),
    tmpFolder: 'tmp',
    clientFolder: 'client',
    adminFolder: 'admin'
}

module.exports = filesConfig